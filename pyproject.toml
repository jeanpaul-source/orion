[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
# Defaults: E4, E7, E9 (pycodestyle errors), F (pyflakes).
# Extend with I (isort) so import order is consistent.
select = ["E4", "E7", "E9", "F", "I"]
