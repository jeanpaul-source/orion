# Development dependencies — not needed in production.
# Install with: pip-sync requirements.lock requirements-dev.lock

pytest>=8.0
ruff>=0.4.0
mypy>=1.10
types-requests>=2.32
pre-commit>=3.7
pytest-cov>=5.0
pip-audit>=2.7
pip-tools>=7.0
